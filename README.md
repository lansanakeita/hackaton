# around_the_corner_hackathon

# Load fixtures
`php bin/console doctrine:fixtures:load`

# Installer les déoendences
`yarn install` ou `npm install`

# lancer le encore webpack
`yarn encore dev-server`